# Program Counter

The **program counter** is a variable whose value encodes the current control state. Given a [[Flow Chart|chart]] with nodes, the program counter takes values corresponding to each node plus a halt state.