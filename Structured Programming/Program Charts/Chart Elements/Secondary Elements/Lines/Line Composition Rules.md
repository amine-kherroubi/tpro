# Line Composition Rules

Multiple input [[Chart Line|lines]] may be joined into a single line. A single line cannot be split into multiple lines. These rules ensure **deterministic** [[Computational History|control flow]] in [[Flow Chart|program charts]].