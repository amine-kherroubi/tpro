# Loop Exit Path

An **exit path** of a [[Chart Loop|loop]] is a [[Chart Path|path]] that:

1. Starts at some [[Chart Point|point]] on the loop (the [[Loop Exit Point|loop exit point]]),
2. Contains no other points from the loop,
3. Ends at the [[Chart Exit Point|exit point]] of the [[Flow Chart|chart]].